# Local benchmark outputs (regenerate with src.run_benchmark)
results/

# Data artifacts (do not publish to GitHub code repo).
# Use a leading slash so src/data/ (provider code) is not ignored.
/data/
/seeds/
/hf_dataset/

# Hugging Face / tooling caches
.cache/
__pycache__/
*.py[cod]
.DS_Store
.env
.env.*

# Local HF upload scratch
.hf_upload_tmp/
