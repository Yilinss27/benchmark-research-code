# Local benchmark outputs (regenerate with src.run_benchmark)
results/

# Data artifacts (do not publish to GitHub code repo)
data/
seeds/
hf_dataset/

# Hugging Face / tooling caches
.cache/
__pycache__/
*.py[cod]
.DS_Store
.env
.env.*

# Local HF upload scratch (optional; hf_dataset/ is the publish package and is tracked)
.hf_upload_tmp/
